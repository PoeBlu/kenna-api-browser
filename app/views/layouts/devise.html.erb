<!DOCTYPE html>
<html>
  <head>
    <%= render 'layouts/head' %>
    <style>
      h2 {
        color:red;
        font-size:16px;
      } 
    </style>
  </head>
  <body style="background: url('https://s3.amazonaws.com/mbrortest/bg.jpg') no-repeat scroll 100%; background-position: top;  font-family: 'GothamBookRegular', sans-serif;">

    <!-- Navbar -->
    <%= render 'layouts/navbar' %>
    
    <!-- Notice/Alert -->
    <%= render 'layouts/notice' %>

    <!-- Info Panel -->
    <% if user_signed_in?  %>
    <div class="row" style="margin:0;">
      <div class="col m6 offset-m3">
        <div class="card" style="color:white; background-color:#34495e; margin-bottom:8px">
          <div class="card-content white-text">
            <span class="card-title">Welcome <%= current_user.email  %>!</span>
            <p>This is a simple demonstration of interacting with the Kenna Security API. <a href="/pages/key">Click here</a> to enter your API Key in the setttings then see the Kenna API in action!.</p>
          </div>
        </div>
      </div>
    </div>
    <% else %>
    <div class="row" style="margin:0;">
      <div class="col m6 offset-m3">
        <div class="card" style="color:white; background-color:#34495e; margin-bottom:8px">
          <div class="card-content white-text">
            <span class="card-title">Kenna Security API Browser</span>
            <p>This is a simple demonstration of interacting with the Kenna Security API.</a></p>
          </div>
        </div>
      </div>
    </div>
    <% end %>
    
    <!-- Divider -->
    <div class="row" width="730px" style="margin:0; padding:0;">
        <div class="col m6 offset-m3"><img width="100%" src="https://s3.amazonaws.com/mbrortest/home-divider%402x.png"></div>
    </div> 
    
    <!-- Page Content -->
    <div class="row" style="margin:0;">
      <div class="col m6 offset-m3">
        <div class="card white" style="margin-top:4px"; margin-bottom:1px>
          <div class="card-content black-text">
              <%= yield %>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Video Image -->
    <div class="row" width="730px">
        <div class="col m6 offset-m3"><img width="100%" src="https://s3.amazonaws.com/mbrortest/home-divider%402x.png"></div>
    </div>  
        
    <!-- Footer -->
    <%= render 'layouts/footer' %>
  </body>
</html>
